apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-fpl-ui
spec:
  rules:
  - host: test.banterdo.me
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-service
            port: 
              number: 80
  ingressClassName: nginx
